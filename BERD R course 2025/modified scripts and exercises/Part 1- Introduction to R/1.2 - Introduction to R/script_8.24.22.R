# new script! 

# the hashtag symbol allows you to enter comments which are ignored when the script is run

5 + 5
8 + 8

# packages

install.packages("dplyr")  # you only need to install a particular package once

library(dplyr)  # you will need to run this line everytime you need to use functions from the dplyr package
